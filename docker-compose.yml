services:
  web:
    build: .
    restart: unless-stopped
    # Dokploy/Traefik reaches the container on this port.
    # Do not bind host :80 — that port belongs to the proxy.
    expose:
      - "80"
