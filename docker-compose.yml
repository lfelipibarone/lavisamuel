services:
  web:
    build: .
    restart: unless-stopped
    # Dokploy: publish container port without binding host :80
    ports:
      - "8080"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
