services:
  web:
    build: .
    restart: unless-stopped
    # Container listens on 8080 (see nginx.conf).
    # In Dokploy Domains, set Service=web and Port=8080.
    # Do not bind host :80 — Traefik owns that.
    ports:
      - "8080"
